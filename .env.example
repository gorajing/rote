# Gemini Computer Use — genai.Client() reads GEMINI_API_KEY
GEMINI_API_KEY=
# Set to 1 to fall back to the legacy gemini-2.5 computer-use model
ROTE_USE_LEGACY_CU=0

# Controlled arena (AcmeBilling)
ROTE_APP_URL=http://localhost:8800

# MongoDB Atlas — vector store with Voyage AI automated embeddings (rote.instructions)
# Setup: Atlas → Search → Embedding Providers → add Voyage AI key, then create autoEmbed index
ROTE_MONGO_URI=

# MiniMax — the second agent in the MCP cross-agent beat (P1)
MINIMAX_API_KEY=
